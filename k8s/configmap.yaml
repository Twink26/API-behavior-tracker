apiVersion: v1
kind: ConfigMap
metadata:
  name: api-tracker-config
  namespace: api-tracker
data:
  DATABASE_URL: "postgresql://api_tracker:password@postgres-service:5432/api_tracker"
  PORT: "5000"
  AWS_REGION: "us-east-1"
  CLOUDWATCH_LOG_GROUP: "api-behavior-tracker"
  CLOUDWATCH_LOG_STREAM: "api-requests"
